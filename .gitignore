# Secrets — never commit
.env

# Production scrape config — leaks internal hostnames. See prometheus-example.yml.
/prometheus.yml

# OS cruft
.DS_Store

# Claude Code / OMC session artifacts
.claude/
.omc/
